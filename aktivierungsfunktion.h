#pragma once

#include "bibliotheken.h"

// Aktivierungsfunktion.
auto f(float x) {
	float b = pow(1 + exp(-x), -1);
	cout << b << endl;

}
// Ableitung der Aktivierungsfunktion.
auto fabl(float x) {

}